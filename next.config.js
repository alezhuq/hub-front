/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["127.0.0.1", "book-back-fymbxxt4va-uc.a.run.app"],
  },
};

module.exports = nextConfig;
